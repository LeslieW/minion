ESSENCE' 1.0

letting  D be domain int(1..4)

find     M : matrix indexed by [D] of D
find     a : D 

such that 

forall i : D .
    ((M[i]*a = i) => (M[i]*a >= 0)), 

forall j : D .
    (M[j]*a - j <= 0)  
