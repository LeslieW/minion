$ Peacable Army of queens
$
$ 'occurrence' representation 

ESSENCE' 1.0



$ board width
given n : int(1..1000)

letting N be domain int(1..n)

$ 0: empty field, 1:white queen, 2: black queen
find board : matrix indexed by [N,N] of int(0..2)


maximising sum row : N .
             sum  col : N .
                board[row,col]

such that

   board[1,1] = 1,  $ let's place a white queen in the north-west corner     

   $ if we have a white queen at position row and column
   $ there is no field on the same row/column/diagonal
   $ that holds a black queen
   forall row,col : N . 
      (board[row, col] = 1) =>
            (forall i : N .
               ((i != row) =>
                  (board[i,col] < 2)) $ instead of != 
               /\
               ((i!=col) =>
                  (board[row,i] < 2))
	       /\ 
               (((row+i <= n) /\ (col+i <= n)) =>
                (board[row+i,col+i] < 2)	  
		)  
	       /\ 
               (((row-i > 0) /\ (col-i > 0)) =>
                (board[row-i,col-i] < 2)	  
		)  
	       /\ 
               (((row+i <= n) /\ (col-i > 0)) =>
                (board[row+i,col-i] < 2)	  
		)  
	       /\ 
               (((row-i > 0) /\ (col+i <= n)) =>
                (board[row-i,col+i] < 2)	  
		))

